{
  "version": "0003",
  "name": "counterparties",
  "transactional": true,
  "preconditionSql": "SELECT to_regclass('public.companies') IS NOT NULL AND NOT EXISTS (SELECT 1 FROM (VALUES ('counterparties'), ('counterparty_roles'), ('customer_profiles'), ('supplier_profiles'), ('customer_ledger_entries'), ('supplier_ledger_entries')) AS expected(relation_name) WHERE to_regclass('public.' || relation_name) IS NOT NULL) AS ok;",
  "verificationSql": "SELECT (SELECT count(*) FROM (VALUES ('counterparties'), ('counterparty_roles'), ('customer_profiles'), ('supplier_profiles'), ('customer_ledger_entries'), ('supplier_ledger_entries')) AS expected(relation_name) WHERE to_regclass('public.' || relation_name) IS NOT NULL) = 6 AS ok;",
  "recovery": "This migration is transactional. Any failure rolls back all 03.B relation creation and the migration-history insert. After successful application, do not edit or drop these relations ad hoc; corrections must be new reviewed forward migrations. PK/FK/UNIQUE/CHECK constraints remain intentionally deferred to 03.06 and all project-owned indexes remain intentionally deferred to 03.07."
}
