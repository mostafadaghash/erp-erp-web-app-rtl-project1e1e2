{
  "version": "0004",
  "name": "product_catalog",
  "transactional": true,
  "preconditionSql": "SELECT to_regclass('public.counterparties') IS NOT NULL AND NOT EXISTS (SELECT 1 FROM (VALUES ('product_categories'), ('products'), ('product_variants'), ('units'), ('product_units'), ('variant_barcodes'), ('attributes'), ('attribute_values'), ('product_attributes'), ('variant_attribute_values'), ('price_lists'), ('price_list_items'), ('reorder_levels')) AS expected(relation_name) WHERE to_regclass('public.' || relation_name) IS NOT NULL) AS ok;",
  "verificationSql": "SELECT (SELECT count(*) FROM (VALUES ('product_categories'), ('products'), ('product_variants'), ('units'), ('product_units'), ('variant_barcodes'), ('attributes'), ('attribute_values'), ('product_attributes'), ('variant_attribute_values'), ('price_lists'), ('price_list_items'), ('reorder_levels')) AS expected(relation_name) WHERE to_regclass('public.' || relation_name) IS NOT NULL) = 13 AS ok;",
  "recovery": "This migration is transactional. Any failure rolls back all 03.C relation creation and the migration-history insert. After successful application, do not edit or drop these relations ad hoc; corrections must be new reviewed forward migrations. PK/FK/UNIQUE/CHECK/generated constraints remain intentionally deferred to 03.06 and all project-owned indexes remain intentionally deferred to 03.07."
}
