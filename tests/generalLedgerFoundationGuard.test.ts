import test from"node:test";import assert from"node:assert/strict";import{readFileSync}from"node:fs";const integration=readFileSync("tests/generalLedgerFoundationIntegration.test.ts","utf8"),matrix=readFileSync("tests/GENERAL_LEDGER_FOUNDATION_COVERAGE_MATRIX.md","utf8"),api=readFileSync("convex/generalLedger.ts","utf8"),helper=readFileSync("convex/lib/generalLedger.ts","utf8");test("guard has forty literal GLF definitions",()=>assert.equal((integration.match(/test\("GLF-/g)??[]).length,40));test("guard matrix has forty executable rows",()=>assert.equal((matrix.match(/^\| GLF-/gm)??[]).length,40));test("guard forbids pending and placeholders",()=>assert.doesNotMatch(matrix+integration,/PENDING|Placeholder|exercise\(\)|case-/));test("guard forbids generated test constructs",()=>assert.doesNotMatch(integration,/\.map\([^\n]*test|\.forEach\([^\n]*test/));test("guard public API has no journal delete",()=>assert.doesNotMatch(api,/export const (update|delete)Journal/));test("guard GL never deletes",()=>assert.doesNotMatch(api+helper,/ctx\.db\.delete/));test("guard balance writes centralized",()=>{assert.doesNotMatch(api,/db\.(insert|patch)\("generalLedger(Account|Period)Balances"/);assert.match(helper,/generalLedgerAccountBalances/)});test("guard operational integration remains false",()=>assert.match(api,/operationalPostingEnabled:false/));
