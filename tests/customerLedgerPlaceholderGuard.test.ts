import test from "node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
test("customer-ledger document scenarios cannot regress to opening-balance placeholders",async()=>{const source=await readFile(new URL("./customerLedgerIntegration.test.ts",import.meta.url),"utf8"),required=new Map([["CL-06","api.invoices.create"],["CL-10","api.invoices.recordPayment"],["CL-12","api.invoices.refundPayment"],["CL-15","api.invoices.cancel"],["CL-16","api.salesReturns.create"],["CL-18","api.salesReturns.reverse"],["CL-19","api.orders.create"],["CL-20","api.orders.addPayment"],["CL-21","api.orders.refundDeposit"],["CL-23","api.repairs.create"],["CL-25","api.repairs.recordPayment"],["CL-26","api.repairs.refundPayment"]]);for(const[id,call]of required){const start=source.indexOf(`test("${id}`),end=source.indexOf("\ntest(\"",start+1),body=source.slice(start,end<0?source.length:end);assert.ok(start>=0,`${id} is missing`);assert.ok(body.includes(call),`${id} must call ${call}`);if(Number(id.slice(3))>=6&&Number(id.slice(3))<=28)assert.equal(body.includes("initializeOpeningBalance"),false,`${id} is an opening-balance placeholder`)}});
