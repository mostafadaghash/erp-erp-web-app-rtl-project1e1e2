# Supplier payments coverage matrix

| ID | البيانات المعدّة | Mutation أو Query المنفذة | آثار قاعدة البيانات المؤكدة | اسم test الفعلي المطابق حرفيًا |
|---|---|---|---|---|
| SPP-01 | بيانات Convex مستقلة للسيناريو: partial payment changes one receipt to partial | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ partial payment changes one receipt to partial | `SPP-01 partial payment changes one receipt to partial` |
| SPP-02 | بيانات Convex مستقلة للسيناريو: full payment changes receipt to paid | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ full payment changes receipt to paid | `SPP-02 full payment changes receipt to paid` |
| SPP-03 | بيانات Convex مستقلة للسيناريو: one payment allocates across two receipts | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ one payment allocates across two receipts | `SPP-03 one payment allocates across two receipts` |
| SPP-04 | بيانات Convex مستقلة للسيناريو: server derives total exclusively from allocations | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ server derives total exclusively from allocations | `SPP-04 server derives total exclusively from allocations` |
| SPP-05 | بيانات Convex مستقلة للسيناريو: treasury decreases by the exact amount | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ treasury decreases by the exact amount | `SPP-05 treasury decreases by the exact amount` |
| SPP-06 | بيانات Convex مستقلة للسيناريو: supplier balance decreases by the exact amount | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ supplier balance decreases by the exact amount | `SPP-06 supplier balance decreases by the exact amount` |
| SPP-07 | بيانات Convex مستقلة للسيناريو: financial transaction is created and linked | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ financial transaction is created and linked | `SPP-07 financial transaction is created and linked` |
| SPP-08 | بيانات Convex مستقلة للسيناريو: supplier ledger entry is created and linked | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ supplier ledger entry is created and linked | `SPP-08 supplier ledger entry is created and linked` |
| SPP-09 | بيانات Convex مستقلة للسيناريو: negative financial movement has the exact amount | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ negative financial movement has the exact amount | `SPP-09 negative financial movement has the exact amount` |
| SPP-10 | بيانات Convex مستقلة للسيناريو: SPY numbers are atomic and unique | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ SPY numbers are atomic and unique | `SPP-10 SPY numbers are atomic and unique` |
| SPP-11 | بيانات Convex مستقلة للسيناريو: same request retry returns the same payment | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ same request retry returns the same payment | `SPP-11 same request retry returns the same payment` |
| SPP-12 | بيانات Convex مستقلة للسيناريو: retry succeeds after receipt became paid | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ retry succeeds after receipt became paid | `SPP-12 retry succeeds after receipt became paid` |
| SPP-13 | بيانات Convex مستقلة للسيناريو: same request with different data is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ same request with different data is rejected | `SPP-13 same request with different data is rejected` |
| SPP-14 | بيانات Convex مستقلة للسيناريو: allocation order is canonical for retries | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ allocation order is canonical for retries | `SPP-14 allocation order is canonical for retries` |
| SPP-15 | بيانات Convex مستقلة للسيناريو: duplicate purchase receipt allocation is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ duplicate purchase receipt allocation is rejected | `SPP-15 duplicate purchase receipt allocation is rejected` |
| SPP-16 | بيانات Convex مستقلة للسيناريو: zero negative and non-finite allocations are rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ zero negative and non-finite allocations are rejected | `SPP-16 zero negative and non-finite allocations are rejected` |
| SPP-17 | بيانات Convex مستقلة للسيناريو: 1.001 is rejected before rounding with no effects | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ 1.001 is rejected before rounding with no effects | `SPP-17 1.001 is rejected before rounding with no effects` |
| SPP-18 | بيانات Convex مستقلة للسيناريو: allocation above receipt remaining amount is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ allocation above receipt remaining amount is rejected | `SPP-18 allocation above receipt remaining amount is rejected` |
| SPP-19 | بيانات Convex مستقلة للسيناريو: payment above supplier balance is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ payment above supplier balance is rejected | `SPP-19 payment above supplier balance is rejected` |
| SPP-20 | بيانات Convex مستقلة للسيناريو: insufficient treasury balance is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ insufficient treasury balance is rejected | `SPP-20 insufficient treasury balance is rejected` |
| SPP-21 | بيانات Convex مستقلة للسيناريو: inactive financial account is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ inactive financial account is rejected | `SPP-21 inactive financial account is rejected` |
| SPP-22 | بيانات Convex مستقلة للسيناريو: financial account from another branch is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ financial account from another branch is rejected | `SPP-22 financial account from another branch is rejected` |
| SPP-23 | بيانات Convex مستقلة للسيناريو: clearing account is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ clearing account is rejected | `SPP-23 clearing account is rejected` |
| SPP-24 | بيانات Convex مستقلة للسيناريو: inactive supplier is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ inactive supplier is rejected | `SPP-24 inactive supplier is rejected` |
| SPP-25 | بيانات Convex مستقلة للسيناريو: receipt belonging to another supplier is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ receipt belonging to another supplier is rejected | `SPP-25 receipt belonging to another supplier is rejected` |
| SPP-26 | بيانات Convex مستقلة للسيناريو: receipt from another branch is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ receipt from another branch is rejected | `SPP-26 receipt from another branch is rejected` |
| SPP-27 | بيانات Convex مستقلة للسيناريو: paid receipt is rejected for a new request | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ paid receipt is rejected for a new request | `SPP-27 paid receipt is rejected for a new request` |
| SPP-28 | بيانات Convex مستقلة للسيناريو: date before finance cutover is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ date before finance cutover is rejected | `SPP-28 date before finance cutover is rejected` |
| SPP-29 | بيانات Convex مستقلة للسيناريو: payment before finance initialization is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ payment before finance initialization is rejected | `SPP-29 payment before finance initialization is rejected` |
| SPP-30 | بيانات Convex مستقلة للسيناريو: user without record supplier payments is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ user without record supplier payments is rejected | `SPP-30 user without record supplier payments is rejected` |
| SPP-31 | بيانات Convex مستقلة للسيناريو: record supplier payments without disbursements is rejected | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ record supplier payments without disbursements is rejected | `SPP-31 record supplier payments without disbursements is rejected` |
| SPP-32 | فرعان نشطان، خزينة ورصيد مورد ومستندا شراء مستقلان، وManager مربوط بالفرع الأول | `supplierPayments.create` لمحاولة صحيحة وثلاث محاولات تجاوز، ثم `list` و`openPurchaseReceipts` | نجاح دفع فرعه؛ رفض branchId والحساب والمستند للفرع الآخر؛ القائمة والمستندات المفتوحة تحتوي الفرع الأول فقط | `SPP-32 manager is isolated from another branch` |
| SPP-33 | فرعان بحسابات وأرصدة ومستندات؛ Admin وAccountant وManager حقيقيون | Admin: `create` و`list` للفرعين ومحاولة خلط؛ Accountant: `create` و`reverse` و`list` وثلاث محاولات تجاوز؛ Manager: `create` و`print` و`reverse` | نجاح Admin في الفرعين مع فصل القوائم ورفض الخلط؛ نجاح إنشاء وعكس Accountant داخل فرعه ورفض تغيير الفرع والحساب والمستند الخارجي وعدم تسريب القائمة؛ نجاح إنشاء وطباعة Manager ورفض العكس | `SPP-33 admin and accountant policy is preserved` |
| SPP-34 | بيانات Convex مستقلة للسيناريو: treasury failure rolls back every effect | `supplierPayments.create` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ treasury failure rolls back every effect | `SPP-34 treasury failure rolls back every effect` |
| SPP-35 | بيانات Convex مستقلة للسيناريو: reversal atomically restores all balances and receipt | `supplierPayments.reverse` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ reversal atomically restores all balances and receipt | `SPP-35 reversal atomically restores all balances and receipt` |
| SPP-36 | بيانات Convex مستقلة للسيناريو: identical reversal retry returns the same result | `supplierPayments.reverse` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ identical reversal retry returns the same result | `SPP-36 identical reversal retry returns the same result` |
| SPP-37 | بيانات Convex مستقلة للسيناريو: reversal retry with different date or reason is rejected | `supplierPayments.reverse` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ reversal retry with different date or reason is rejected | `SPP-37 reversal retry with different date or reason is rejected` |
| SPP-38 | بيانات Convex مستقلة للسيناريو: different reversal request is rejected and bidirectional links remain | `supplierPayments.reverse` | فحص سجلات قاعدة البيانات والنتيجة الخاصة بـ different reversal request is rejected and bidirectional links remain | `SPP-38 different reversal request is rejected and bidirectional links remain` |
| SPP-39 | فرعان، حساب ورصيد لكل فرع، مستندات متعددة، سندان في الأول وسند في الثاني | `list` بـ `numItems: 1` و`continueCursor` حتى `isDone`، و`receiptHistory` كمستخدم فرع وكـAdmin | صفحتا الفرع الأول لا تحتويان سند الثاني وتنتهي Pagination؛ DTOs بلا الحقول المحظورة؛ تاريخ الفرع الأول متاح، تاريخ الفرع الثاني مرفوض لغير Admin ومتاح للـAdmin | `SPP-39 payment pagination and receipt history are branch isolated` |
| SPP-40 | سند فعلي وProfiles تطابق `createdBy` تتابعيًا عبر userId ثم token ثم بلا تطابق | `supplierPayments.print` ثلاث مرات؛ التنفيذ يستخدم `by_user` ثم `by_token` دون Full Table Scan | اسم `by_user` ثم اسم fallback `by_token` ثم «مستخدم غير معروف» بلا المعرف الخام؛ مفاتيح DTO محدودة ولا توجد الحقول الداخلية المحظورة؛ غير المصرح له مرفوض | `SPP-40 protected print returns a limited voucher DTO` |
