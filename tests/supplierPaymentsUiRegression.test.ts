import test from "node:test"; import assert from "node:assert/strict"; import { readFile } from "node:fs/promises";
test("supplier payment UI uses dedicated APIs, gated picker, stable ids and error helper",async()=>{const s=await readFile("src/components/SupplierPaymentsPage.tsx","utf8");for(const token of ["api.supplierPayments.create","api.supplierPayments.reverse","api.finance.disbursementAccountPicker","canRecord && canDisburse","createRequest.current","reverseRequest.current","getErrorMessage","if (busy","if (!canPrint)"])assert.ok(s.includes(token),token);for(const forbidden of ["totalAmount:","supplierName:","receiptNumber:","currentBalance","availableBalance","pendingBalance","as any","@ts-ignore"])assert.equal(s.includes(forbidden),false,forbidden);});
