import test from"node:test";import assert from"node:assert/strict";import{readFileSync}from"node:fs";const ui=readFileSync("src/components/GeneralLedgerPage.tsx","utf8");
test("UI-01 Arabic RTL",()=>assert.match(ui,/dir="rtl"/));test("UI-02 foundation banner",()=>assert.match(ui,/Foundation/));test("UI-03 automation disabled notice",()=>assert.match(ui,/غير مفعّل/));test("UI-04 status",()=>assert.match(ui,/generalLedger.status/));test("UI-05 account picker",()=>assert.match(ui,/accountPicker/));test("UI-06 periods",()=>assert.match(ui,/generalLedger.periods/));test("UI-07 initialize mutation",()=>assert.match(ui,/generalLedger.initialize/));test("UI-08 manual journal mutation",()=>assert.match(ui,/postManualJournal/));test("UI-09 stable request ref",()=>assert.match(ui,/useRef\(newRequestId/));test("UI-10 rotates request success",()=>assert.match(ui,/request.current=newRequestId/));test("UI-11 busy guard",()=>assert.match(ui,/if\(busy\)return/));test("UI-12 real Convex errors",()=>assert.match(ui,/getErrorMessage/));test("UI-13 no prompt",()=>assert.doesNotMatch(ui,/window\.prompt/));test("UI-14 debit preview",()=>assert.match(ui,/المدين:/));test("UI-15 credit preview",()=>assert.match(ui,/الدائن:/));test("UI-16 difference preview",()=>assert.match(ui,/الفرق:/));test("UI-17 blocks imbalance",()=>assert.match(ui,/difference!==0/));test("UI-18 EGP",()=>assert.match(ui,/EGP/));
