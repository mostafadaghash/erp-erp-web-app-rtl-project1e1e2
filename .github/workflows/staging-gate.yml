name: Staging Release Gate

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: staging-gate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: quality
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Repository verification
        run: npm run verify

  browser-e2e:
    name: browser-e2e
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: staging
    env:
      STAGING_ENVIRONMENT: staging
      STAGING_BASE_URL: ${{ vars.STAGING_BASE_URL }}
      E2E_PRODUCT_QUERY: ${{ vars.E2E_PRODUCT_QUERY }}
      E2E_REQUIRE_ALL_ROLES: "true"
      E2E_ROLE_ACCOUNTS_JSON: ${{ secrets.E2E_ROLE_ACCOUNTS_JSON }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Install pinned Playwright runner
        run: npm install --no-save --package-lock=false @playwright/test@1.62.1
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Validate isolated staging target and role matrix
        run: npm run staging:check
      - name: Authenticated browser acceptance
        run: npm run test:e2e:staging
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            playwright-report
            test-results/e2e
          if-no-files-found: ignore
          retention-days: 7

  release-gate:
    name: release-gate
    needs: [quality, browser-e2e]
    if: ${{ always() }}
    runs-on: ubuntu-latest
    steps:
      - name: Block release unless all gates passed
        env:
          QUALITY_RESULT: ${{ needs.quality.result }}
          E2E_RESULT: ${{ needs.browser-e2e.result }}
        run: |
          test "$QUALITY_RESULT" = "success"
          test "$E2E_RESULT" = "success"
          echo "Staging release gate passed."
